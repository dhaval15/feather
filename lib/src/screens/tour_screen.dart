import 'package:flutter/material.dart';
import '../utils/utils.dart';

class TourScreen extends StatelessWidget with ScreenUtilMixin {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
